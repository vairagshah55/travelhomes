const axios = require('axios');// Create an axios instance with default configconst api = axios.create({  baseURL);// Add a request interceptor to include auth tokenapi.interceptors.request.use(  (config) => {    // const token = localStorage.getItem('adminToken');    // Server environment does not have localStorage! Obtain token another way if needed.    // Example, or set in .env for test/dev; leave commented for safety.    // if (token) {    //   config.headers.Authorization = `Bearer ${token}`;    // }    return config;  },  (error) => {    return Promise.reject(error);  });// User API servicesconst userService = {  // Get all users with optional status filter  getUsers: async (status) => {    try {      const response = await api.get('/admin/users', {        params);      return response.data;    } catch (error) {      throw (error )?.response?.data || (error )?.message || String(error);    }  },  // Get a single user by ID  getUser: async (id) => {    try {      const response = await api.get(`/admin/users/${id}`);      return response.data;    } catch (error) {      throw (error )?.response?.data || (error )?.message || String(error);    }  },  // Create a new user  createUser: async (userData) => {    try {      const response = await api.post('/admin/users', userData);      return response.data;    } catch (error) {      throw (error )?.response?.data || (error )?.message || String(error);    }  },  // Update an existing user  updateUser: async (id) => {    try {      const response = await api.put(`/admin/users/${id}`, userData);      return response.data;    } catch (error) {      throw (error )?.response?.data || (error )?.message || String(error);    }  },  // Delete a user  deleteUser: async (id) => {    try {      const response = await api.delete(`/admin/users/${id}`);      return response.data;    } catch (error) {      throw (error )?.response?.data || (error )?.message || String(error);    }  },};// Management API servicesconst managementService = {  // Get all management listings with optional status filter  getManagementListings: async (status) => {    try {      const response = await api.get('/admin/management', {        params);      return response.data;    } catch (error) {      throw (error )?.response?.data || (error )?.message || String(error);    }  },  // Get a single management listing by ID  getManagementListing: async (id) => {    try {      const response = await api.get(`/admin/management/${id}`);      return response.data;    } catch (error) {      throw (error )?.response?.data || (error )?.message || String(error);    }  },  // Create a new management listing  createManagementListing: async (listingData) => {    try {      const response = await api.post('/admin/management', listingData);      return response.data;    } catch (error) {      throw (error )?.response?.data || (error )?.message || String(error);    }  },  // Update an existing management listing  updateManagementListing: async (id) => {    try {      const response = await api.put(`/admin/management/${id}`, listingData);      return response.data;    } catch (error) {      throw (error )?.response?.data || (error )?.message || String(error);    }  },  // Delete a management listing  deleteManagementListing: async (id) => {    try {      const response = await api.delete(`/admin/management/${id}`);      return response.data;    } catch (error) {      throw (error )?.response?.data || (error )?.message || String(error);    }  },  // Update management listing status  updateListingStatus: async (id) => {    try {      const response = await api.patch(`/admin/management/${id}/status`, { status });      return response.data;    } catch (error) {      throw (error )?.response?.data || (error )?.message || String(error);    }  },};module.exports = api;